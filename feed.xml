<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://notnanton.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://notnanton.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-03-13T09:12:04+00:00</updated><id>https://notnanton.github.io/feed.xml</id><title type="html">blank</title><subtitle>AI Researcher and Entrepreneur. Co-Founder of Pharos Labs and AdaLab. Taking a closer look at what makes us human. </subtitle><entry><title type="html">Real-Robot Learning on a Budget: Setup, Stack, and Techniques</title><link href="https://notnanton.github.io/blog/2026/robot-learning-setup/" rel="alternate" type="text/html" title="Real-Robot Learning on a Budget: Setup, Stack, and Techniques"/><published>2026-03-13T00:00:00+00:00</published><updated>2026-03-13T00:00:00+00:00</updated><id>https://notnanton.github.io/blog/2026/robot-learning-setup</id><content type="html" xml:base="https://notnanton.github.io/blog/2026/robot-learning-setup/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>This post describes the technical setup behind my robot learning experiments. It’s written as a standalone reference for anyone who wants to understand the components, the techniques, and how they fit together.</p> <p>The goal is to teach a low-cost robot arm to perform manipulation tasks with minimal human effort. The approach combines imitation learning from demonstrations with reinforcement learning for online improvement. Everything runs on a single-GPU workstation and a 120-euro robot arm.</p> <p>For the narrative of how we got here — the timeline, the dead ends, the breakthroughs — see the <a href="/blog/2026/robot-rl-experiments/">companion post</a>.</p> <h2 id="hardware">Hardware</h2> <h3 id="robot-arm">Robot Arm</h3> <p>The <strong>SO-101</strong> is an open-source 6-DOF robot arm with a parallel-jaw gripper, built from 3D-printed parts and Feetech STS3215 servo motors. It communicates over a single USB-to-serial connection using the Feetech protocol.</p> <p>Key properties:</p> <ul> <li>6 joints: pan, tilt, elbow (2 servos in differential), roll, gripper</li> <li>Position control at up to 30 Hz</li> <li>Current (torque) sensing via <code class="language-plaintext highlighter-rouge">Present_Current</code> register — used for reward shaping and safety</li> <li>Low cost (~120 EUR for parts), but mechanically imprecise — backlash and compliance vary across units</li> </ul> <p>We call ours “shabby” for a reason. The imprecision is actually a feature for research: methods that work on this hardware should transfer easily to better arms.</p> <h3 id="sensors">Sensors</h3> <p><strong>Camera.</strong> A single USB camera mounted on the wrist (first-person view / FPV), capturing 640x480 RGB at 30 fps. Images are downsampled to 128x128 for policy input (or 64x64 for some RL experiments).</p> <p>FPV-only is a deliberate constraint. Most prior work uses fixed overhead cameras that see the entire workspace. With FPV, the robot can only see what’s in front of its gripper, making tasks like “find the object” require memory or exploration. This is harder but more general — a mobile robot wouldn’t have an overhead camera either.</p> <p><strong>Proprioception.</strong> Joint positions (6 DOF) and motor currents (6 channels), both read from the servo bus at the control frequency.</p> <h3 id="teleoperation">Teleoperation</h3> <p>Demonstrations are collected via a <strong>leader-follower</strong> setup: a second SO-101 arm (the leader) is physically moved by a human. The follower arm mirrors the leader’s joint positions in real time. Joint positions and camera images are recorded as a LeRobot dataset at the control FPS.</p> <p>This approach produces natural, kinematically consistent demonstrations without any inverse kinematics or motion planning. The downside is that the human can’t feel what the follower is touching (no force feedback).</p> <h2 id="software-stack">Software Stack</h2> <p>Everything builds on <a href="https://github.com/huggingface/lerobot"><strong>LeRobot</strong></a>, HuggingFace’s open-source robot learning framework. We maintain a fork with extensions for:</p> <ul> <li><strong>Joint-space control</strong> in the gym environment (upstream only supported end-effector / Cartesian)</li> <li><strong>Delta action conversion</strong> with interpolation for large movements</li> <li><strong>Reward classifier training and evaluation</strong> from demonstration datasets</li> <li><strong>Human-in-the-loop reward feedback</strong> via keyboard during RL</li> <li><strong>Torque penalty and safety layers</strong> using motor current sensing</li> <li><strong>XQC policy</strong> (Cross-Q with Corrections — distributional SAC variant)</li> <li><strong>Combined Experience Replay</strong> mixing recent transitions with replay buffer</li> <li><strong>Rerun integration</strong> for real-time visualization of images, actions, and metrics</li> </ul> <p>Training and logging use <strong>PyTorch</strong> and <strong>Weights &amp; Biases</strong>. For rapid prototyping, we also have <code class="language-plaintext highlighter-rouge">minimal_rl.py</code>, a standalone single-process SAC script that bypasses the distributed architecture and talks to the robot directly.</p> <h2 id="imitation-learning">Imitation Learning</h2> <p>We train visuomotor policies from demonstrations using two architectures.</p> <h3 id="act">ACT</h3> <p><strong>Action Chunking with Transformers</strong> <d-cite key="zhao2023act"></d-cite> is a CVAE-based policy. The encoder is a ResNet backbone processing camera images and a transformer that fuses visual and proprioceptive tokens. The decoder predicts a <em>chunk</em> of future actions (typically 50-100 timesteps) in one forward pass.</p> <p>Key properties for our setup:</p> <ul> <li>ResNet backbone uses pretrained ImageNet weights</li> <li>Works well with small datasets (40-100 demonstrations)</li> <li>Predicts absolute joint positions</li> <li>Training is straightforward: L1 reconstruction loss on action chunks</li> <li>Default <code class="language-plaintext highlighter-rouge">n_action_steps=50</code> means the robot executes ~5 seconds of open-loop control per prediction at 10 Hz. Reducing to 5-10 gives more reactive behavior.</li> </ul> <h3 id="smolvla">SmolVLA</h3> <p><strong>SmolVLA</strong> <d-cite key="allal2025smolvla"></d-cite> adapts a vision-language model (VLM) backbone for robot control. The architecture reuses a pretrained SmolVLM-2 and fine-tunes it to output motor commands.</p> <p>Key properties:</p> <ul> <li>Pretrained VLM backbone — this is critical. Training from scratch (which we accidentally did for months) produces near-random policies.</li> <li>Designed for 512x512 image input, though we often train at 128x128 for speed.</li> <li>More parameter-efficient fine-tuning via LoRA/PEFT is possible but still under development.</li> <li>Better sample efficiency on some tasks due to the pretrained visual features, but more sensitive to hyperparameters than ACT.</li> </ul> <h3 id="training-details">Training Details</h3> <table> <thead> <tr> <th>Setting</th> <th>ACT</th> <th>SmolVLA</th> </tr> </thead> <tbody> <tr> <td>Learning rate</td> <td>1e-5</td> <td>1e-4</td> </tr> <tr> <td>Batch size</td> <td>8-64</td> <td>64</td> </tr> <tr> <td>Training steps</td> <td>10k-100k</td> <td>15k-200k</td> </tr> <tr> <td>Image resolution</td> <td>dataset native</td> <td>128x128-512x512</td> </tr> <tr> <td>Pretrained backbone</td> <td>ResNet (ImageNet)</td> <td>SmolVLM-2</td> </tr> <tr> <td>Action chunk length</td> <td>50-100</td> <td>50</td> </tr> </tbody> </table> <p>Findings from our experiments:</p> <ul> <li><strong>Batch size matters more than training steps.</strong> ACT at bs=64 for 10k steps matched or exceeded bs=1 at 100k steps (4-5/10 vs. 1/10 success rate on cube sorting), reaching the same loss ~10x faster.</li> <li><strong>Overfitting is the main enemy.</strong> SmolVLA clearly overfits past 100k steps on our dataset sizes (40-100 episodes). Validation loss tracking and early stopping with EMA weight averaging are essential.</li> <li><strong>Loss correlates with performance</strong>, at least within the same architecture. This isn’t obvious — the training loss is reconstruction error on action chunks, not task success.</li> <li><strong>FPV-only is limited by context.</strong> With a single wrist camera and <code class="language-plaintext highlighter-rouge">n_obs_steps=1</code>, the robot has no memory. Increasing context to 16 frames (~1.6s) helps but isn’t enough for tasks requiring search. We likely need 2-5 seconds or a fundamentally different architecture (recurrent, xLSTM).</li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset=" /assets/img/robot-rl/il-training-loss-comparison-480.webp 480w, /assets/img/robot-rl/il-training-loss-comparison-800.webp 800w, /assets/img/robot-rl/il-training-loss-comparison-1400.webp 1400w, " sizes="95vw" type="image/webp"/> <img src="/assets/img/robot-rl/il-training-loss-comparison.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture><figcaption class="caption">WandB training loss (action chunk MSE) on the cube sorting task. Three ACT runs (orange, green, red) at different learning rates and batch sizes plateau around loss 1.5-2.0, while SmolVLA (teal) converges to near zero. The gap comes from the pretrained VLM backbone: SmolVLA starts with strong visual features, so the policy head only needs to learn the action mapping. All runs use the same 40-episode FPV dataset.</figcaption> </figure> <figure> <picture> <source class="responsive-img-srcset" srcset=" /assets/img/robot-rl/il-validation-loss-overfitting-480.webp 480w, /assets/img/robot-rl/il-validation-loss-overfitting-800.webp 800w, /assets/img/robot-rl/il-validation-loss-overfitting-1400.webp 1400w, " sizes="95vw" type="image/webp"/> <img src="/assets/img/robot-rl/il-validation-loss-overfitting.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture><figcaption class="caption">Validation loss (computed with actual inference logic, not teacher-forced) for a SmolVLA training run on cube sorting. Loss decreases until around 60k steps, then starts climbing and becomes noisy past 80k. The vertical line marks the best checkpoint. This motivated adding early stopping with EMA weight averaging to all our training scripts. With only 40-100 demos, overfitting is the main failure mode.</figcaption> </figure> <h2 id="reward-models">Reward Models</h2> <p>For RL, we need a reward signal. We use three approaches, often in combination.</p> <p><strong>Learned reward classifier.</strong> A ResNet-10 binary classifier trained on demonstration images. The last N frames of each successful demonstration are labeled <code class="language-plaintext highlighter-rouge">reward=1.0</code>, everything else <code class="language-plaintext highlighter-rouge">reward=0.0</code>. The classifier predicts task success from a single camera image.</p> <p>Performance is task-dependent. For tasks with clear visual contrasts (matchbox on a bag vs. empty surface) it reaches ~0.7 confidence on true positives. For cluttered scenes with distracting objects, false positive rates increase. We use <code class="language-plaintext highlighter-rouge">pos_weight</code> to handle class imbalance in the training loss.</p> <p><strong>Torque-based rewards.</strong> Motor currents are a cheap proxy for undesirable contact. High currents usually mean the robot is pushing against something (the table, itself, a joint limit). The penalty is:</p> \[\text{penalty} = w \cdot \sum_{i} I_i^2 / 1000\] <p>where \(I_i\) is the current on motor \(i\) and \(w\) is a tunable weight. The gripper motor can be excluded since gripping <em>should</em> produce current.</p> <p><strong>Human-in-the-loop rewards.</strong> During live RL training, a human operator presses number keys (0-9) to provide graded rewards, or minus (-) for punishment. This is surprisingly effective for shaping early behavior and doesn’t require any visual model.</p> <h2 id="reinforcement-learning">Reinforcement Learning</h2> <h3 id="sac">SAC</h3> <p>The core RL algorithm is <strong>Soft Actor-Critic</strong> <d-cite key="haarnoja2018sac"></d-cite>, an off-policy maximum-entropy method. It jointly trains:</p> <ul> <li>An <strong>actor</strong> (policy network): outputs a squashed Gaussian distribution over delta joint actions</li> <li>A <strong>twin critic</strong>: two Q-networks that estimate expected return, taking the minimum to prevent overestimation</li> <li>A <strong>temperature</strong> \(\alpha\): automatically tuned to target a desired entropy level</li> </ul> <p>We use standard SAC with several additions:</p> <ul> <li><strong>Delayed policy updates</strong> (TD3-style): the actor updates every 4 critic updates, which helps stability</li> <li><strong>Weight normalization</strong>: after each gradient step, Linear layer weights (except output heads) are projected to unit norm. This keeps the effective learning rate constant.</li> <li><strong>Update-to-data (UTD) ratio of 4</strong>: 4 gradient steps per environment step. This is important because environment steps are expensive (real-time on hardware), so we want to extract as much learning as possible from each transition.</li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset=" /assets/img/robot-rl/rl-base-learning-curves-480.webp 480w, /assets/img/robot-rl/rl-base-learning-curves-800.webp 800w, /assets/img/robot-rl/rl-base-learning-curves-1400.webp 1400w, " sizes="95vw" type="image/webp"/> <img src="/assets/img/robot-rl/rl-base-learning-curves.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture><figcaption class="caption">SAC on real hardware: position holding with 6 joint angles as input. Top panel: reward (negative squared distance to home position) converges to near zero within 800 steps. Second panel: Q-value estimates stabilize around -150 to -200. Third: torque readings stay moderate (0.5-1.5 range). Fourth: control loop frequency holds at 6-8 Hz. Bottom: the entropy coefficient alpha decays from 0.20 to 0.12 as the policy becomes more deterministic. This was our first successful end-to-end RL run on the real robot.</figcaption> </figure> <figure> <picture> <source class="responsive-img-srcset" srcset=" /assets/img/robot-rl/rl-weight-norm-learning-curves-480.webp 480w, /assets/img/robot-rl/rl-weight-norm-learning-curves-800.webp 800w, /assets/img/robot-rl/rl-weight-norm-learning-curves-1400.webp 1400w, " sizes="95vw" type="image/webp"/> <img src="/assets/img/robot-rl/rl-weight-norm-learning-curves.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture><figcaption class="caption">Same position holding task with weight normalization and torque penalty added. Now tracking six panels: total reward, position reward component, torque penalty (near zero, meaning the robot holds position with minimal force), Q-values (much smaller magnitude around -5 to -8 vs. -200 without weight norm), torque, and control frequency. The reward signal is noisier with periodic dips from exploration, but Q-values are better calibrated and the torque penalty stays flat. Alpha decays more slowly, suggesting the policy maintains useful exploration longer. Run converges in about 1600 steps.</figcaption> </figure> <h3 id="action-spaces">Action Spaces</h3> <p><strong>Absolute vs. delta.</strong> Demonstrations are recorded as absolute joint positions, but the RL policy outputs delta actions: small increments relative to the current position. This makes exploration local and safe.</p> \[a_{\text{target}} = q_{\text{current}} + \text{tanh}(\pi(s)) \cdot \Delta_{\max}\] <p>where \(\Delta_{\max} = \text{action_scale_per_s} / \text{fps}\) (default: 50/10 = 5.0 position-units per step).</p> <p><strong>Demo conversion.</strong> When seeding the replay buffer from demonstrations, absolute-position transitions are converted to deltas. If any delta exceeds the action scale, the transition is split into N sub-transitions with linearly interpolated proprioception and zero-order-hold images. Without this, the policy would be asked to learn impossibly large single-step jumps.</p> <h3 id="torque-aware-safety">Torque-Aware Safety</h3> <p>The robot is fragile. We implement two safety layers:</p> <p><strong>Proactive.</strong> Every delta action is clamped to \(\pm 5°\) per step, preventing large sudden movements regardless of what the policy outputs.</p> <p><strong>Reactive.</strong> Motor currents are read each step. When current exceeds a threshold, the commanded action is attenuated. Movements back toward the home position are exempted from attenuation so the robot can recover from loaded positions.</p> <p>These safety measures run at the environment level and are transparent to the policy. The policy sees only that certain actions produce less movement than expected.</p> <h3 id="human-in-the-loop-rewards">Human-in-the-Loop Rewards</h3> <p>During live RL, a human watches the robot and presses keys to provide reward:</p> <table> <thead> <tr> <th>Key</th> <th>Reward</th> </tr> </thead> <tbody> <tr> <td>0-9</td> <td>0.0 to 0.9 (graded)</td> </tr> <tr> <td>-</td> <td>-0.5 (punishment)</td> </tr> </tbody> </table> <p>The human reward is added to whatever automated signal exists (torque penalty, classifier). This makes it possible to start training from scratch without any reward model — the human provides the learning signal until an automated reward becomes reliable.</p> <h3 id="xqc">XQC</h3> <p><strong>Cross-Q with Corrections</strong> <d-cite key="bhatt2024crossq"></d-cite> is an extension of SAC that we’ve implemented but not yet deployed on the real robot. The key ideas:</p> <ul> <li><strong>No target networks.</strong> Instead, batch normalization is applied to a <em>joined</em> batch of current and next states during the critic forward pass. This ensures consistent normalization statistics and removes the need for a separate target network and its soft update.</li> <li><strong>Distributional critic (C51).</strong> Instead of predicting a single Q-value, the critic outputs a categorical distribution over returns (51 atoms). The loss is a categorical cross-entropy against a projected Bellman target. This captures uncertainty and tends to learn faster.</li> <li><strong>Weight normalization.</strong> After each optimizer step, all hidden-layer weights are projected to the unit sphere, keeping the effective learning rate stable regardless of weight magnitude.</li> </ul> <p>The implementation is complete and tested, but batch normalization with batch_size=1 during inference (when the robot takes a single action) requires careful handling of BN running statistics. This is the main blocker for real-robot deployment.</p> <h2 id="experimental-settings">Experimental Settings</h2> <p>We’ve been working bottom-up, starting with the simplest possible RL problems on real hardware before adding complexity.</p> <p><strong>Imitation learning task: pick and place.</strong> Grab a cube (or matchbox) and place it at a target position. Trained from 40-100 teleoperated demonstrations using FPV camera input. This is the main benchmark for our IL policies (ACT, SmolVLA).</p> <p><strong>RL task 1: Position holding (joints only).</strong> The simplest possible RL problem. The reward is the negative squared distance from the current joint positions to a home position. No camera, just 6 joint angles as input. The policy learns to stay still despite exploration noise. Converges in ~800 steps. This validated that our SAC implementation, reward loop, and robot communication all work correctly.</p> <p><strong>RL task 2: Torque minimization (joints only).</strong> Same as above but with a torque penalty added: the policy is penalized for high motor currents. This teaches the robot to hold position while using minimal force. The task revealed that unconstrained exploration can put dangerous pressure on the servos, which led us to implement the PI compliance controller and the reactive safety layer.</p> <p><strong>Next RL tasks (planned):</strong></p> <ul> <li>Position holding with vision — same reward, but the policy receives camera images instead of (or in addition to) joint positions. A stepping stone to visual RL.</li> <li>Vision-based pick and place — combining the IL task with online RL improvement via ResFiT or direct fine-tuning.</li> </ul> <h2 id="open-problems">Open Problems</h2> <p>These are the questions I’m currently working on or planning to tackle, roughly in priority order.</p> <p><strong>Residual fine-tuning (ResFiT).</strong> Freeze a pretrained IL policy, train a small residual correction on top with SAC. This approach keeps the structure of demonstrations while allowing RL to fix systematic errors. Prior work shows 13% to 64% success rate improvements with 15 minutes of real-world RL <d-cite key="ankile2025resfit"></d-cite>.</p> <p><strong>RL with visual observations.</strong> All current RL experiments use only proprioception (joint positions). Extending to camera-conditioned policies requires a visual encoder in the actor and critic, data augmentation (DrQ-style), and likely much more data. The IL policies already use vision, so the question is how to transfer that capability to the RL setting.</p> <p><strong>Temporal context for FPV.</strong> A single frame from a wrist camera doesn’t contain enough information for many tasks. Options include: stacking past frames as transformer context, using recurrent architectures (xLSTM looks promising for its constant memory and efficient inference), or maintaining an explicit spatial memory.</p> <p><strong>Hierarchical RL.</strong> For tasks with sparse rewards and long horizons, flat RL is unlikely to work. We’re sketching a two-level architecture: a high-level manager that selects discrete goal codes every K steps, and a low-level worker that executes. The worker gets dense goal-reaching rewards; the manager handles sparse task rewards. Hindsight relabeling of the manager’s goals provides additional training signal.</p> <p><strong>Better reward models.</strong> The current binary classifier is too coarse. Directions include: dense rewards from visual features (color histograms, optical flow), active querying to improve the model where it’s uncertain, and multi-modal rewards that combine vision with proprioception and torque.</p>]]></content><author><name>Anton Wiehe</name></author><category term="reinforcement-learning"/><category term="robotics"/><category term="imitation-learning"/><summary type="html"><![CDATA[A technical overview of my robot learning setup — hardware, software, imitation learning, reward models, and reinforcement learning on a real SO-101 arm.]]></summary></entry><entry><title type="html">Robot RL: From Demonstrations to Online Learning on a Real SO-101</title><link href="https://notnanton.github.io/blog/2026/robot-rl-experiments/" rel="alternate" type="text/html" title="Robot RL: From Demonstrations to Online Learning on a Real SO-101"/><published>2026-03-13T00:00:00+00:00</published><updated>2026-03-13T00:00:00+00:00</updated><id>https://notnanton.github.io/blog/2026/robot-rl-experiments</id><content type="html" xml:base="https://notnanton.github.io/blog/2026/robot-rl-experiments/"><![CDATA[<h2 id="overview">Overview</h2> <p>This post covers about seven months of work on getting a real robot arm to learn manipulation tasks. It’s meant as an honest account of the process — the dead ends, the debugging, and the things that eventually clicked. The goal is to give my professor and potential collaborators a clear picture of where the project stands, what infrastructure exists, and where students could plug in.</p> <p>For a cleaner technical overview of the setup, techniques, and algorithms, see the <a href="/blog/2026/robot-learning-setup/">companion post</a>. This post is the lab diary.</p> <p>The high-level goal is language-guided goal setting with quick learning of new tasks. The first concrete sub-goal was simple: grab a matchbox and place it somewhere. That turned out to be far from simple.</p> <p>Everything is built on <a href="https://github.com/huggingface/lerobot">LeRobot</a> (HuggingFace’s robot learning framework), with extensive modifications to support joint-space control, delta action conversion, human reward feedback, torque-aware safety, and various RL improvements.</p> <h2 id="hardware-and-software">Hardware and Software</h2> <ul> <li><strong>Robot</strong>: SO-101 follower arm (“shabby”), 6 DOF + gripper, Feetech STS3215 servos</li> <li><strong>Teleoperation</strong>: SO-101 leader arm for demonstration collection</li> <li><strong>Camera</strong>: Single front-view (FPV) camera, 640x480, downsampled to 128x128 for policy input</li> <li><strong>Compute</strong>: NVIDIA GPU for training, robot control on CPU</li> <li><strong>Framework</strong>: LeRobot (fork), PyTorch, WandB for logging</li> <li><strong>Location</strong>: <a href="https://attraktor.org/">Attraktor</a> makerspace in Hamburg</li> </ul> <h2 id="timeline">Timeline</h2> <h3 id="aug-sep-2025-getting-started-with-il">Aug-Sep 2025: Getting started with IL</h3> <p>We started at the Attraktor makerspace, trying to get imitation learning running on the matchbox task. The first few weeks were mostly fighting with the infrastructure:</p> <ul> <li>Dataset collection worked but had issues: weird channel ordering, corrupted recordings (40 episodes recorded but only 3 MP4 files locally), and manual version tagging</li> <li>The reward classifier trained but was unreliable — many false positives</li> <li>ACT training worked but evaluation on the robot failed due to hardcoded feature dimensions (6 joints expected, but 12 with velocity features)</li> <li>We merged upstream LeRobot changes and hit more compatibility issues — no support for non-end-effector control in <code class="language-plaintext highlighter-rouge">gym_manipulator.py</code></li> </ul> <p>By September we had SmolVLA and ACT training on 40 FPV episodes. Under 500 training steps nothing happened, but from 1000 steps both showed signs of reacting to the matchbox. SmolVLA at 10k steps actually managed to grasp once and place it, though it seemed random.</p> <h3 id="oct-nov-2025-first-rl-attempts-and-overfitting">Oct-Nov 2025: First RL attempts and overfitting</h3> <p>October brought the first RL attempts, which immediately ran into a memory problem: the LeRobot replay buffer stored images 3 times (480x640x3 for 10k frames = 34 GB). We had to divide the dataset by 8 to make it fit, and even then the actor script crashed the machine.</p> <p>We also discovered that SmolVLA defaults to executing 50 actions per prediction (~5 seconds of open-loop control at 10 Hz). Reducing <code class="language-plaintext highlighter-rouge">n_action_steps</code> to 5-10 made the policy much more reactive, and was essential for tasks requiring closed-loop behavior.</p> <p>On the IL side, we discovered that batch size matters a lot. We trained ACT and SmolVLA at various settings:</p> <ul> <li>200k steps SmolVLA lr=1e-4 bs=64 — overfitting, just repeated motions without looking</li> <li>200k steps ACT lr=1e-5 bs=64 — poor performance</li> <li>100k steps ACT lr=1e-5 bs=1 — first good FPV policy, actually grasped objects</li> </ul> <p>But the story got more nuanced. In November, we did more systematic comparisons and found that bs=64 ACT at 10k steps performed comparably or better than bs=1 at 100k (4-5/10 success vs. 1/10), reaching the same loss much faster. The key insight: loss does seem to correlate with performance, but both overfit eventually. SmolVLA trained for 500k steps was clearly overfit past 100k.</p> <figure> <picture> <source class="responsive-img-srcset" srcset=" /assets/img/robot-rl/il-training-loss-comparison-480.webp 480w, /assets/img/robot-rl/il-training-loss-comparison-800.webp 800w, /assets/img/robot-rl/il-training-loss-comparison-1400.webp 1400w, " sizes="95vw" type="image/webp"/> <img src="/assets/img/robot-rl/il-training-loss-comparison.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture><figcaption class="caption">WandB training loss on the cube sorting task. Three ACT runs (orange, green, red) with different hyperparameters all plateau around loss 1.5-2.0. SmolVLA (teal) drops to near zero thanks to the pretrained VLM backbone. At the time, we thought lower loss automatically meant better task performance. It mostly did within the same architecture, but comparing across architectures was misleading.</figcaption> </figure> <figure> <picture> <source class="responsive-img-srcset" srcset=" /assets/img/robot-rl/il-validation-loss-overfitting-480.webp 480w, /assets/img/robot-rl/il-validation-loss-overfitting-800.webp 800w, /assets/img/robot-rl/il-validation-loss-overfitting-1400.webp 1400w, " sizes="95vw" type="image/webp"/> <img src="/assets/img/robot-rl/il-validation-loss-overfitting.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture><figcaption class="caption">Validation loss for a SmolVLA run. Loss bottoms out around 60k steps (vertical line marks the best checkpoint), then climbs steadily past 80k. With only 40-100 demos, overfitting is inevitable if you train too long. This plot is what convinced us to add early stopping with EMA weight averaging to every training script.</figcaption> </figure> <p>This was also when we recognized a fundamental limitation of FPV-only control: with a single wrist camera, the robot can’t see the full scene. We need temporal context (multiple past observations), but on our compute budget we could only train with ~16 past frames — about half a second. We probably need 2-5 seconds of context.</p> <p>We also set up WandB for shared logging and added validation loss tracking to detect overfitting properly, using actual inference logic rather than training loss to make different architectures comparable.</p> <h3 id="nov-2025-the-pretrained-weights-discovery">Nov 2025: The pretrained weights discovery</h3> <p>By late November I was genuinely frustrated — five months in and struggling to get consistent performance. Then came a painful realization: we had been training SmolVLA completely from scratch — not even using the pretrained VLM weights. The PEFT policy was producing outputs close to the default calibration position, which tipped us off. Months of SmolVLA experiments had been running without the pretrained backbone.</p> <p>After fixing this, we recorded a much larger dataset (nearly 100 episodes of cube sorting with 4 cubes), added proper early stopping with EMA weight averaging, and started contributing fixes back upstream (validation loss tracking, notes on pretrained weight defaults).</p> <h3 id="jan-2026-renavigating">Jan 2026: Renavigating</h3> <p>After a break, we came back with clearer goals:</p> <ul> <li>Debug the context-16 observation window (it wasn’t working correctly)</li> <li>Get RL running with a pretrained policy and human-in-the-loop rewards</li> <li>Decide between training a reward function vs. pure human feedback</li> </ul> <p>We also started contributing back to LeRobot: validation loss PR, PEFT training fixes, and various compatibility patches.</p> <h3 id="jan-feb-2026-first-rl-that-works">Jan-Feb 2026: First RL that works</h3> <p>Late January was the breakthrough. Key findings:</p> <ul> <li>The ResNet reward classifier still wasn’t reliable — too many false positives</li> <li>We added torque reading as a reward signal (negative reward for high motor current, except gripper)</li> <li>We implemented UMAP visualization of trajectories to understand what the policy was exploring</li> </ul> <p>We also studied the ResFiT paper (Ankile et al. 2025) in detail and identified concrete things to implement: n-step returns, higher UTD ratio (we were at ~0.2 effective when training at 2Hz but operating at 10Hz), delayed policy updates, and DrQ augmentations.</p> <p>On February 13 — a long night session — we mapped out the full ResFiT architecture in detail: freeze the IL policy, predict its action for each sample, let SAC learn small residual corrections bounded to 20% of action range, use a shallow ViT with DrQ augmentations for the RL critic, layer norm only in the critic, update actor every 2-8 critic steps, and sample 50/50 from expert and newly collected data. Their results with ACT on real hardware were compelling: 1000 demos plus 134 online RL episodes (15 minutes) improved a bimanual pick-and-place from 13% to 64% success.</p> <p>I also attended the Mannheim RL Workshop 2026, which solidified my thinking around algorithm design: separating the core algorithm from stabilization techniques (e.g., replay buffer is fundamental, target network is a stabilizer). Met the Darmstadt RL group (Jan Peters) and picked up the idea of using future state entropy as an exploration signal.</p> <h3 id="feb-mar-2026-minimal-rl-and-compliance">Feb-Mar 2026: Minimal RL and compliance</h3> <p>The distributed actor-learner architecture was too complex for debugging. So I wrote <code class="language-plaintext highlighter-rouge">minimal_rl.py</code> — a single-process SAC script that bypasses all the infrastructure and talks directly to the robot.</p> <p>I started with the simplest possible problem: hold the home position. Reward = negative distance to home, joints only as input, no camera. The policy learned in about 800 steps. Learning rate wasn’t very sensitive — anything from 1e-2 to 3e-4 worked, with 1e-4 converging fastest.</p> <figure> <picture> <source class="responsive-img-srcset" srcset=" /assets/img/robot-rl/rl-base-learning-curves-480.webp 480w, /assets/img/robot-rl/rl-base-learning-curves-800.webp 800w, /assets/img/robot-rl/rl-base-learning-curves-1400.webp 1400w, " sizes="95vw" type="image/webp"/> <img src="/assets/img/robot-rl/rl-base-learning-curves.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture><figcaption class="caption">The first successful RL run on the real robot. Five panels from our minimal_rl.py script: reward (negative distance to home) converges to near zero within 800 steps, Q-values stabilize around -200, torque stays moderate, the control loop holds 6-8 Hz, and the entropy coefficient alpha decays as the policy commits to staying still. Simple task, but this validated the entire pipeline: SAC, robot communication, reward loop, and safety layer all working together.</figcaption> </figure> <p>Then I added torque minimization as a second objective. The policy learned to hold position with minimal force, but the experiments revealed that RL exploration can put real strain on the servos. That directly motivated the next step.</p> <p>In early March I integrated a PI compliance controller — the robot gives way to resistance instead of fighting it, with a reactive safety layer that reads motor currents and attenuates commands when torque is too high. This made experimentation much safer.</p> <p>I also added weight normalization (projecting linear layer weights to unit sphere after each gradient step, XQC-style) and delayed policy updates (TD3-style, update actor every 4 critic updates). Both helped stability.</p> <figure> <picture> <source class="responsive-img-srcset" srcset=" /assets/img/robot-rl/rl-weight-norm-learning-curves-480.webp 480w, /assets/img/robot-rl/rl-weight-norm-learning-curves-800.webp 800w, /assets/img/robot-rl/rl-weight-norm-learning-curves-1400.webp 1400w, " sizes="95vw" type="image/webp"/> <img src="/assets/img/robot-rl/rl-weight-norm-learning-curves.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture><figcaption class="caption">Position holding with weight normalization and torque penalty. Now six panels: total reward, position reward, torque penalty (near zero, meaning the robot holds with minimal force), Q-values (much better calibrated at -5 to -8 vs. -200 without weight norm), torque readings, and control frequency. The reward is noisier due to periodic exploration dips, but the Q-values are realistic and the torque penalty stays flat. Converges in about 1600 steps.</figcaption> </figure> <p>The next step is adding vision to these simple tasks (position holding with camera input) before moving to the real target: RL-based improvement of the pick-and-place policies.</p> <h2 id="the-pipeline">The Pipeline</h2> <p>The full pipeline has three stages. In practice, I’ve been mostly working with the minimal RL script for stage 3.</p> <h3 id="step-1-demonstration-collection">Step 1: Demonstration Collection</h3> <p>Leader-follower teleoperation: physically move the leader arm and the follower mirrors. Episodes are saved as LeRobot datasets with joint positions, camera images, and timestamps.</p> <p>Controls during recording:</p> <ul> <li><strong>Right Arrow</strong>: save episode</li> <li><strong>Left Arrow</strong>: discard and re-record</li> <li><strong>Escape</strong>: stop</li> </ul> <p>I typically collect 20-50 demonstrations per task. The data is stored as absolute joint positions at the recording FPS (usually 10 or 30).</p> <h3 id="step-2-reward-labeling-and-classifier-training">Step 2: Reward Labeling and Classifier Training</h3> <p>A post-processing script marks the last N frames of each successful episode as <code class="language-plaintext highlighter-rouge">reward=1.0</code>, everything else as <code class="language-plaintext highlighter-rouge">reward=0.0</code>. A ResNet-10 classifier trains on these labeled images.</p> <p>The reward classifier’s accuracy depends heavily on the task. For tasks with clear visual contrasts (e.g., matchbox on a bag vs. empty surface) it works reasonably well. For more ambiguous scenes with distracting objects, false positives become an issue. Human reward feedback and torque-based rewards are complementary signals we use alongside it.</p> <h3 id="step-3-reinforcement-learning">Step 3: Reinforcement Learning</h3> <p>Two options:</p> <p><strong>Distributed (LeRobot’s actor-learner architecture)</strong>: Actor runs on the robot, learner trains SAC, they communicate over gRPC. This is the full-featured version with offline replay buffer seeding, reward classifiers, and human intervention via keyboard. It works but is complex to debug.</p> <p><strong>Minimal single-process (<code class="language-plaintext highlighter-rouge">minimal_rl.py</code>)</strong>: Everything in one script. Currently my main tool for rapid iteration. See next section.</p> <h2 id="current-setup--minimal-rl">Current Setup — Minimal RL</h2> <p>The script I’m actively using is <code class="language-plaintext highlighter-rouge">minimal_rl.py</code> — a ~1000-line single-process SAC implementation that talks directly to the robot hardware. Current default configuration:</p> <table> <thead> <tr> <th>Parameter</th> <th>Value</th> <th>Notes</th> </tr> </thead> <tbody> <tr> <td>FPS</td> <td>10</td> <td>Control loop frequency</td> </tr> <tr> <td>Learning rate</td> <td>3e-4</td> <td>Adam, same for all networks</td> </tr> <tr> <td>Batch size</td> <td>64</td> <td> </td> </tr> <tr> <td>Buffer size</td> <td>10,000</td> <td>Simple deque</td> </tr> <tr> <td>Discount</td> <td>0.99</td> <td> </td> </tr> <tr> <td>Tau</td> <td>0.005</td> <td>Soft target update rate</td> </tr> <tr> <td>UTD ratio</td> <td>4</td> <td>Critic updates per env step</td> </tr> <tr> <td>Warmup</td> <td>50 steps</td> <td>Random actions before learning</td> </tr> <tr> <td>Episode length</td> <td>200 steps</td> <td>Max steps per episode</td> </tr> <tr> <td>Policy delay</td> <td>4</td> <td>Actor update every 4 critic updates</td> </tr> <tr> <td>Weight norm</td> <td>On</td> <td>XQC-style projection to unit sphere</td> </tr> <tr> <td>Batch norm</td> <td>Off</td> <td>Fragile with batch_size=1 inference</td> </tr> <tr> <td>Reward norm</td> <td>Off</td> <td>Running mean/std — still buggy</td> </tr> <tr> <td>Mixed precision</td> <td>bf16</td> <td>On CUDA</td> </tr> </tbody> </table> <p>The architecture is simple: a 256-dim encoder (Linear + LayerNorm + ReLU), a 2-layer actor MLP outputting tanh-squashed Gaussian actions, and a twin-critic with 2-layer MLPs. Joint positions are the input (6 DOF, normalized to [-1, 1]). Camera input can be enabled (and was used for all supervised learning with ACT/SmolVLA), but is off by default in the minimal RL script for the simple proprioceptive tasks we’ve been debugging with. Getting RL to work with visual observations on the real robot is an active next step.</p> <p>Reward is currently: <code class="language-plaintext highlighter-rouge">reward = -distance_to_home / 100 - torque_penalty</code>. The torque penalty uses the sum of squared motor currents.</p> <p>On Ctrl+C, the script automatically generates:</p> <ul> <li>Learning curves (reward, Q-values, torque, entropy temperature)</li> <li>Heatmaps showing critic Q-values and actor responses across the joint space</li> <li>UMAP visualizations of visited states using policy distribution distances</li> </ul> <h2 id="key-technical-decisions">Key Technical Decisions</h2> <h3 id="delta-actions-and-interpolation">Delta Actions and Interpolation</h3> <p>Demonstrations are recorded as absolute joint positions, but the RL policy operates in delta space. The conversion:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>delta = (action - current_position) / action_scale
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">action_scale = action_scale_per_s / fps</code> (default: 50/10 = 5.0 per step). The policy outputs in [-1, 1], which maps to a maximum of 5 position-units per step.</p> <p>When converting offline demos, if any delta exceeds the action scale, the transition is split into N sub-transitions with interpolated proprioception and zero-order-hold images. This was essential — without it, the policy was trying to learn impossible single-step jumps.</p> <h3 id="torque-penalties-and-safety">Torque Penalties and Safety</h3> <p>Motor currents are read via <code class="language-plaintext highlighter-rouge">Present_Current</code> from the Feetech bus. The torque penalty uses a softplus:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>penalty = scale * log(1 + exp(0.5 * (sum_I_squared/1000 - 10)))
</code></pre></div></div> <p>There’s also a safety layer that clamps max delta per step (5 degrees) and attenuates commands when current is already high. Movements toward home are exempted from current attenuation so the robot can recover from gravity-loaded positions.</p> <h3 id="weight-normalization">Weight Normalization</h3> <p>After each gradient step, all Linear layer weights (except output heads) are projected onto the unit sphere. This is from the XQC paper and helps with training stability by keeping the effective learning rate constant. I have a full XQC implementation (distributional C51 critic with categorical cross-entropy loss, batch normalization with joined forward passes) ready but haven’t deployed it on the real robot yet — the batch norm is fragile with batch_size=1 during inference.</p> <h3 id="human-reward-feedback">Human Reward Feedback</h3> <p>During RL training, number keys 0-9 provide graded rewards (0.0 to 0.9), minus key gives punishment (-0.5). This works alongside or instead of the reward classifier. The rewards are added to whatever other reward signal exists (torque penalty, classifier) within the same step.</p> <h2 id="what-worked-and-what-didnt">What Worked and What Didn’t</h2> <p><strong>Worked well:</strong></p> <ul> <li>Leader-follower teleoperation — reliable and intuitive</li> <li>Small ACT policies (bs=1, 100k steps) — first real success on the matchbox task</li> <li>Delta action interpolation — essential for bridging IL demos and RL</li> <li>Minimal single-process RL — dramatically faster iteration than distributed</li> <li>Weight normalization — noticeable stability improvement</li> <li>Torque penalty — effective at preventing self-damage during exploration</li> <li>PI compliance controller — makes the robot safe to be around</li> </ul> <p><strong>Still in progress:</strong></p> <ul> <li>Reward classifier — performance is task-dependent; works well for clear visual contrasts, needs tuning for ambiguous scenes</li> <li>Batch normalization in RL — implemented but not yet properly tested on the real robot due to batch_size=1 inference complications</li> <li>Reward normalization — running mean/std needs more work to stabilize in early training</li> <li>RL with vision — supervised learning with camera images works (that’s how ACT/SmolVLA operate), but we haven’t gotten RL to work well with visual observations yet</li> </ul> <p><strong>Didn’t work well:</strong></p> <ul> <li>SmolVLA from scratch — we accidentally trained without pretrained weights for months</li> <li>Large batch sizes for IL — overfit faster than expected</li> <li>Distributed actor-learner — functionally correct but too complex to debug; the minimal single-process script was the fix</li> </ul> <h2 id="whats-next">What’s Next</h2> <p><strong>Immediate (next weeks):</strong></p> <ul> <li><strong>ResFiT (Residual fine-tuning)</strong>: Freeze a pretrained ACT/SmolVLA policy, learn small residual corrections with SAC. The paper (Ankile et al. 2025) shows improvements from 13% to 64% success rate with only 15 minutes of real-world RL. We have the full architecture mapped out.</li> <li><strong>XQC on real hardware</strong>: The distributional critic (C51) and batch norm architecture is implemented and tested in simulation. Need to solve the inference-mode BN issue and deploy.</li> <li><strong>Visual RL tasks</strong>: Move beyond joint-only observations to camera-conditioned policies for real manipulation (matchbox grasp, cube sorting).</li> </ul> <p><strong>Medium-term:</strong></p> <ul> <li><strong>Hierarchical RL</strong>: A two-level Director architecture where a high-level manager picks discrete goal codes every K steps and a low-level worker executes. This addresses the sparse reward problem in real-robot tasks. The worker trains with dense goal-reaching rewards while the manager handles sparse task rewards. We’ve sketched out hindsight relabeling and competence-based curriculum learning.</li> <li><strong>Temporal context for FPV</strong>: Single-frame FPV is fundamentally limited. Exploring xLSTM as an alternative to transformers for temporal processing — constant memory, efficient inference, potentially infinite context. Could replace or augment SmolVLA’s architecture.</li> <li><strong>Better reward signals</strong>: Dense rewards from visual features (color histograms, optical flow), learned reward models with active querying.</li> </ul> <p><strong>Longer-term:</strong></p> <ul> <li><strong>Mannheim RL Workshop 2026</strong>: Targeting a presentation or poster on the real-robot RL pipeline.</li> <li><strong>Language-conditioned goal setting</strong>: The original high-level goal — tell the robot what to do in natural language and have it learn quickly.</li> </ul> <p><strong>Contributions back to LeRobot (PRs in progress or planned):</strong></p> <ul> <li>Validation loss tracking with proper inference-time evaluation</li> <li>PEFT fine-tuning fixes and documentation for pretrained weight defaults</li> <li>Joint-space control in gym_manipulator</li> <li>Various compatibility patches between versions</li> </ul> <p><strong>Where students could plug in:</strong></p> <ul> <li>ResFiT implementation and real-robot evaluation (needs someone comfortable with PyTorch and willing to be hands-on with hardware)</li> <li>Reward model improvements — the classifier is the weakest link</li> <li>Hierarchical RL / goal-conditioned policies — mostly a research question, well-suited for a thesis</li> <li>xLSTM or other temporal architectures for FPV-only control</li> <li>Multi-camera or depth perception (we’ve been FPV-only, adding a second camera would change a lot)</li> </ul>]]></content><author><name>Anton Wiehe</name></author><category term="reinforcement-learning"/><category term="robotics"/><summary type="html"><![CDATA[Seven months of building a robot learning pipeline — the timeline, the dead ends, and the breakthroughs. See also the companion technical overview post.]]></summary></entry><entry><title type="html">My PhD Vision</title><link href="https://notnanton.github.io/blog/2024/phd_vision/" rel="alternate" type="text/html" title="My PhD Vision"/><published>2024-01-01T00:00:00+00:00</published><updated>2024-01-01T00:00:00+00:00</updated><id>https://notnanton.github.io/blog/2024/phd_vision</id><content type="html" xml:base="https://notnanton.github.io/blog/2024/phd_vision/"><![CDATA[<h2 id="overview">Overview</h2> <h3 id="goal">Goal</h3> <p>An intelligent agent in direct interaction with its environment, capable of quick in-context learning.</p> <h3 id="strategy">Strategy</h3> <p>Set up a cognitive architecture with a plan to train and evolve it. For the PhD: focus on individual aspects and study how they compose in concrete projects.</p> <p>Concrete plan:</p> <ol> <li>Get a suitable testing ground (robot with good simulation, or a decent grid world).</li> <li>Survey existing AGI architectures and plans (Sutton’s Alberta Plan, Schmidhuber’s Big Net, LeCun’s JEPA, etc). Write a paper summarizing the key ingredients and where they diverge.</li> <li>Design my own plan and describe in detail how the modules interact.</li> <li>Work on the most promising module. Current bet: meta-learning with adapters and mixture-of-experts.</li> <li>Select up to 3 modules that can be researched during the PhD and publish them along with the overarching architectural plan.</li> </ol> <h2 id="cognitive-architecture">Cognitive Architecture</h2> <h3 id="concept">Concept</h3> <p>The idea is an overall cognitive architecture made of specialized but interconnected modules, loosely inspired by how the brain organizes perception, motivation, and action. Below I sketch the modules and the role each one plays.</p> <h3 id="individual-modules">Individual Modules</h3> <p><strong>Policy network(s).</strong> A division of labor: multiple policy networks, each responsible for different actuator regions, but with overall connectedness between them. Think of it like the motor cortex, where regions specialize but still coordinate.</p> <p><strong>Value network.</strong> Outputs a multi-faceted reward signal. The analogy here is the brain’s control of endorphins and other neurochemicals. Rather than a single scalar reward, the value network produces a richer signal that captures different dimensions of “how are things going.”</p> <p><strong>Intrinsic motivation network(s).</strong> Separate from external reward. These networks generate internal rewards for things like curiosity, pain avoidance, beauty-seeking, and self-preservation. The point is that an agent shouldn’t need an external teacher to want to explore or to avoid breaking itself.</p> <p><strong>Perception network.</strong> Produces the input for the policy network by building an internal model of the world. It applies sensory biases to filter noise and generates a coherent, multi-modal integration of all senses. This module should be very robust and change slowly, mostly at the beginning of “life” and through evolutionary pressure.</p> <p><strong>Affordance network.</strong> Takes in the current state and an intention, then projects what actions are possible and what their consequences look like. This could be a conditional mode within the perception network, or a standalone module. It answers the question: “given where I am and what I want, what can I do?”</p> <p><strong>World model network.</strong> Strongly connected to the affordance network. Predicts the next state given current state and action. This is what lets the agent plan by simulating futures before committing to action.</p> <h3 id="learning-hierarchies">Learning Hierarchies</h3> <p>Learning happens at different levels, each on its own timescale:</p> <ol> <li> <p><strong>Evolutionary.</strong> Defines base reactive behavior, main cognitive biases, and architectural improvements. Implemented as an evolutionary algorithm, guided by epigenetic variations (for example, network node stability and importance influencing how much weights are allowed to change, similar to ideas in the Uber AI paper on protecting learned knowledge).</p> </li> <li> <p><strong>Lifelong.</strong> Integration of facts, long-term memory, core behaviors, and automatisms. The policy network is trained here, possibly through meta-learning over situational episodes. This is where skills become permanent.</p> </li> <li> <p><strong>Situational.</strong> Quick adaptation using adapters (like LoRAs). The agent encounters a new situation and rapidly adjusts. If an adapter proves useful enough over time, it gets integrated into the base network, graduating from situational to lifelong knowledge.</p> </li> </ol> <h2 id="research-directions">Research Directions</h2> <p>A few directions I find most promising, roughly grouped:</p> <p><strong>Incorporating foundation model knowledge into RL agents.</strong> Can we use what CLIP or LLMs already know and plug it into a reinforcement learning agent? There are several angles: using them as a knowledge base, repurposing their token embeddings as a latent space for “thinking,” or treating their learned algorithms as general-purpose tools even in non-language domains.</p> <p><strong>Planning as internal simulation.</strong> Put planning steps (the interaction between a world model and a policy) into recurrent memory so the agent can “think” before acting. It simulates paths, evaluates them, and only commits to action once planning is done.</p> <p><strong>Hierarchical RL with learned abstractions.</strong> Hierarchical actor-critic methods where options and sub-tasks are discovered automatically through quantized, symbol-like representations (in the spirit of VQ-VAE). The goal is an agent that builds its own action hierarchy rather than having one hand-designed.</p> <p><strong>Meta-learning for faster adaptation.</strong> Defining learnability itself through meta-learning: training a world model that learns how to learn, so the agent can pick up new tasks with minimal experience. Related: dividing datasets into clusters, training with MAML across them as separate “skills,” and increasing cluster granularity over time.</p>]]></content><author><name>Anton Wiehe</name></author><category term="agi"/><category term="cognition"/><summary type="html"><![CDATA[Where I want to go with my research and how I plan to get there]]></summary></entry></feed>